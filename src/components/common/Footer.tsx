/**
 * Footer — static app footer, rendered once by AppLayout.
 */
export default function Footer() {
  return (
    <footer className="mt-8 border-t py-6 text-center text-xs text-muted-foreground">
      <p>E-Commerce • API Test Harness</p>
      <p className="mt-1 opacity-70">UI strictly reflects backend API capabilities</p>
    </footer>
  );
}
