# Named build mode for the CI agent-browser job: real minified assets served
# by `vite preview`, in-app mock data, no backend. The production guard in
# vite.config.ts only rejects mock in `production` mode — this mode exists so
# dogfood never weakens that guard.
VITE_USE_MOCK=true
